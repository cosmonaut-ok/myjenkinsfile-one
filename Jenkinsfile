#!/usr/bin/env groovy

shit {
    stages {
        stage('test') {
            steps {
                sh 'echo "execute say hello script:"'
                // body()
                // sayHello("Peter")
            }
        }
    }
}
